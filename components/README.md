# Components Directory

This directory contains React UI components for the HILDA interface.

## Structure

- **ui/**: Reusable UI components (buttons, inputs, cards, etc.)
- **features/**: Feature-specific components
- **layouts/**: Layout components

## Usage

All components should be built with TypeScript and styled with Tailwind CSS.
